{
  "name": "lighthouse-diff",
  "version": "0.1.0",
  "description": "Compare Lighthouse scores between commits, CI-ready",
  "type": "module",
  "bin": {
    "lighthouse-diff": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "action.yml"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["lighthouse", "performance", "ci", "diff", "github-action"],
  "author": "Katie",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "lighthouse": "^12.0.0",
    "puppeteer": "^22.0.0",
    "simple-git": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}
